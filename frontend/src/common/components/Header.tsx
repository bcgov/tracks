import React from 'react';

const Header = () => (
  <div id="header">
    <img src="/images/gov3_bc_logo.png" width={155} height={52} alt={'BC Government Logo'} id="logo"/>
      <h2 id="mainTitle">Tracks</h2>
  </div>
);

export default Header;
